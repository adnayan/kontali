import { expect } from "chai";

import { Scraper } from "../../src/classes/Scraper";

describe("Testing class Scraper.", () => {
  let scrapter: Scraper;
  beforeEach(() => {
    scrapter = new Scraper();
  });
});
